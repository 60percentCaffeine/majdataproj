# Tools

`patch-melonloader-043.ps1` needs `Mono.Cecil.dll` at runtime.

The working copy used `Mono.Cecil.dll` from the MelonLoader v0.5.7 release package. The DLL itself is intentionally gitignored because it is an external binary.

Expected local path:

```text
sample-mod/tools/Mono.Cecil.dll
```
