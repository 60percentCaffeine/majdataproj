# Progress

- 2026-06-01 - Create test hook mod health endpoint and readiness file: implemented `TestHookMod`, build/install scripts, readiness docs, verified in MajdataPlay that `ready.json` is written and Windows `GET /health` returns structured JSON.
- 2026-06-01 - Add bridge configuration precedence: implemented defaults, config-file, environment, and CLI precedence for host/port/REPL; exposed effective settings in readiness and health JSON; verified defaults, config, env override, CLI override, and invalid-config startup failure in MajdataPlay.
- 2026-06-01 - Implement isolated Roslyn eval on Unity main thread: added `POST /eval-isolated`, Roslyn `csc.dll` isolated snippet compilation, Unity-thread scheduling through the captured synchronization context, async `await Task.FromResult(...)` support, bridge globals/utilities, and structured compilation/execution errors; verified success, async success, and compilation-error responses in MajdataPlay.
- 2026-06-01 - Add persistent eval session resettable through HTTP: added `POST /eval` with replayed persistent imports/statements, `POST /reset-session`, and distinct isolated eval behavior; verified persistent variables, persistent imports, isolated failure to see session variables, and reset clearing state in MajdataPlay.
- 2026-06-01 - Add bounded JSON serialization for eval results: replaced simple eval result formatting with bounded primitive/array/dictionary/object serialization, cycle `$ref` markers, unsupported runtime value records, and `phase: serialization` errors for maxDepth/maxResponseBytes limits; verified primitives, arrays, dictionaries, DTOs, cycles, delegates, depth limits, and byte limits in MajdataPlay.
- 2026-06-01 - Build separate Windows REPL client: added the Test Hook Mod REPL console app, build/install scripts, `/eval` input flow, `:reset`, `:clear`, `:exit`, and `:help`; test hook startup launches the installed client by default and honors `--modtest-no-repl`; verified manual client eval/reset and default/disabled launch behavior in MajdataPlay.
- 2026-06-01 - Create reusable bridge TestClient and TestHarness: added `ModTestHarness` library with endpoint wrappers for health/eval/eval-isolated/reset/shutdown, game-directory launch via `start-controller.bat`, fresh readiness polling, log collection, and readiness-PID shutdown fallback; verified the library builds in Release.
- 2026-06-01 - Add graceful bridge shutdown endpoint: implemented `POST /shutdown` with structured accepted/fallbackPid JSON, main-thread Unity quit request, and delayed in-process termination fallback for this game profile; verified the endpoint returns accepted and the MajdataPlay PID exits.
- 2026-06-01 - Split sample mod into core, Melon adapter, and unit tests with coverage: added `TestMod.Core`, xUnit tests, `test-unit.ps1` coverage command, and updated the Melon adapter build/install to delegate through the core assembly; verified 3 unit tests pass with Cobertura coverage and the adapter installs with `TestMod.Core.dll`.
- 2026-06-01 - Add sample integration test command for real MajdataPlay: added `test-integration.ps1` and xUnit integration tests that build/install bridge, REPL client, harness, and sample mod, launch through `start-controller.bat`, wait for readiness/health, assert `TestMod.Core` through `/eval-isolated`, request shutdown with PID fallback, and collect logs; verified the integration command passes with one xUnit test.
- 2026-06-01 - Add per-mod aggregate test entrypoint: added `test-all.ps1` to run unit coverage and real-game integration tests in sequence while preserving unit coverage and integration log artifacts.
- 2026-06-01 - Issue 09, Expand sample mod invariant regression tests: expanded `mod-test-tools/integration/SampleModIntegrationTests.cs` from a single core eval smoke test into an invariant suite that verifies `TestMod` and `TestMod.Core` assemblies are loaded inside MajdataPlay, reflection can discover `TestMod.TestMod` and `TestMod.Core.TestModLogic`, and core behavior returns `StartupMessage=Loaded`, `AddScore(100,25)=125`, and negative delta remains `100`. The test now always shuts down and collects `.scratch/mod-test-tools-artifacts/integration` logs, then scans `*.log` artifacts for sample-mod startup failures and common unhandled exception shapes (`TestMod failed`, `Unhandled Exception`, `NullReferenceException`, type/load/method failures). Verification: `dotnet build` for `mod-test-tools/integration/ModTest.Integration.Tests.csproj` passed with 0 warnings/errors; `sample-mod/test-integration.ps1` passed 1/1 xUnit test in 8s and wrote `.scratch/mod-test-tools-artifacts/integration-test-results/integration.trx`. Note: the install step still emits the existing REPL `SYSLIB0014` warning from `WebRequest.Create`; downstream log/error canaries may want a broader game/MelonLoader fatal-error taxonomy than this sample-mod-focused scan.
- 2026-06-01 - Issue 10, Add game boot stable state canary: added `GameBootCanaryTests.GameBootReachesStableRunningUnityState`, shared integration eval/log assertions, and disabled xUnit parallelization for the real-game integration assembly because all tests share one MajdataPlay install and bridge port. The boot canary launches through the harness, polls `/eval-isolated` until two consecutive samples show the same non-empty active scene, loaded scene state, advancing `UnityEngine.Time.frameCount`, increasing realtime, `Application.isPlaying`, and a present/writable `Time.timeScale` API, then shuts down and scans `.scratch/mod-test-tools-artifacts/boot-stable-state/*.log` for fatal boot lines. Also fixed two quality issues found while making the canary meaningful: `TestMod.Core.dll` now installs under `Mods/TestModLib` with a `TestMod` assembly resolver instead of the root `Mods` folder, eliminating MelonLoader's support-DLL load error; the test hook now treats missing `UnityEngine.Application.Quit` as an expected fallback path and relies on forced process termination without logging `MissingMethodException`. Hardened `ModTestHarness` to serialize runtime ownership by killing stale `MajdataPlay`/REPL processes before launch, retrying locked readiness-file cleanup, waiting for shutdown, and cleaning runtime processes after each test. Verification: `dotnet build mod-test-tools/integration/ModTest.Integration.Tests.csproj -c Release --nologo` passed with 0 warnings/errors; `sample-mod/test-integration.ps1` passed 2/2 xUnit tests in 24s and wrote `.scratch/mod-test-tools-artifacts/integration-test-results/integration.trx`, plus current log artifacts under `.scratch/mod-test-tools-artifacts/integration` and `.scratch/mod-test-tools-artifacts/boot-stable-state`. Caveat: this Unity profile exposes `Time.timeScale` as writable but not readable in managed metadata, so the canary verifies the time-scale API presence/writability and uses frame/realtime advancement for the actual liveness signal; `.log.old` files may retain earlier failed-run lines but the current `*.log` artifacts were clean.
- 2026-06-01 - Issue 11, Add core assemblies and game types canary: added `GameTypeCanaryTests.CoreAssembliesAndRepresentativeGameTypesAreLoadable`, a non-mutating AppDomain/type-resolution probe that runs inside MajdataPlay through `/eval-isolated`. It verifies 5 representative assemblies are available (`Assembly-CSharp`, `MajSimai`, `UnityEngine.CoreModule`, `Unity.InputSystem`, `ManagedBass`) and resolves 9 representative types across manager/input/audio/chart/gameplay surfaces: `MajdataPlay.GameManager`, `MajdataPlay.IO.InputManager`, `MajdataPlay.IO.AudioManager`, `MajdataPlay.Settings.ChartSetting`, `MajSimai.SimaiParser`, `MajdataPlay.Scenes.Game.GamePlayManager`, `MajdataPlay.Scenes.Game.NoteLoader`, `MajdataPlay.Scenes.Game.Notes.Controllers.NoteManager`, and `MajdataPlay.Scenes.Game.Buffers.NoteInfo`. Missing assembly/type failures are returned as newline-delimited names in assertion messages, and the test does not instantiate game types or call their methods. Verification: `dotnet build mod-test-tools/integration/ModTest.Integration.Tests.csproj -c Release --nologo` passed with 0 warnings/errors; `sample-mod/test-integration.ps1` passed 3/3 serialized real-game xUnit tests in 43s and wrote `.scratch/mod-test-tools-artifacts/integration-test-results/integration.trx`; current `game-types`, `integration`, and `boot-stable-state` `*.log` artifacts had no fatal/error scan hits. Caveat: this canary deliberately checks loadability and type names only; behavior of those systems is left for the later focused input/audio/chart/gameplay canaries.
- 2026-06-01 - Issue 12, Add scene object invariant canary: added `SceneObjectCanaryTests.BootedSceneHasBroadUnityAndMajdataObjectInvariants`, a read-only live-scene probe using `UnityEngine.Resources.FindObjectsOfTypeAll` and the active scene roots after boot. It verifies broad minimums instead of exact counts: non-empty active scene name, at least one active root object, at least one active/enabled camera, at least one active/enabled audio listener, active UI surface via canvas or event system, active `MajdataPlay.MajComponent` instances, and at least one stable MajdataPlay root/controller component among `GameManager`, `TitleManager`, or `ListManager`. The test avoids instantiating objects, calling controller methods, or mutating scene state; it only counts active components. Verification: `dotnet build mod-test-tools/integration/ModTest.Integration.Tests.csproj -c Release --nologo` passed with 0 warnings/errors; `sample-mod/test-integration.ps1` passed 4/4 serialized real-game xUnit tests in 46s and wrote `.scratch/mod-test-tools-artifacts/integration-test-results/integration.trx`; current `scene-objects`, `game-types`, `integration`, and `boot-stable-state` `*.log` artifacts had no fatal/error scan hits. Caveat: this canary intentionally records no brittle exact object counts, so downstream UI/gameplay tests should add focused assertions only after they intentionally navigate to a specific scene.
- 2026-06-01 - Issue 13, Add game asset and data load canary: added `GameAssetDataCanaryTests.KnownChartMetadataAndAssetPathsResolveAfterBoot`, a read-only metadata/path canary centered on the stable local/built-in `MAJTITLE` chart. It waits up to 60s for TitleManager's delayed chart scan to populate `SongStorage`, then verifies `SongStorage` has collections, non-empty collections, non-zero `TotalChartCount`, enumerable songs, and a `MAJTITLE` metadata record with artist `bbben`, a non-empty hash, and at least 5 levels. It also verifies concrete local and built-in asset paths resolve: `MaiCharts/Original/MAJTITLE/maidata.txt`, `track.mp3`, `bg.png`, built-in `StreamingAssets/MaiCharts/Original/MAJTITLE/maidata.txt`, built-in `track.opus`, and `Skins/default/TapSkins/tap.png`; the local `maidata.txt` is read only to confirm `&title=MAJTITLE`. Verification: `dotnet build mod-test-tools/integration/ModTest.Integration.Tests.csproj -c Release --nologo` passed with 0 warnings/errors; `sample-mod/test-integration.ps1` passed 5/5 serialized real-game xUnit tests in 1m24s and wrote `.scratch/mod-test-tools-artifacts/integration-test-results/integration.trx`; current `asset-data`, `scene-objects`, `game-types`, `integration`, and `boot-stable-state` `*.log` artifacts had no fatal/error scan hits. Surprise: bridge readiness happens before `SongStorage.InitAsync` completes, so the canary must poll for chart metadata readiness rather than sample immediately; downstream data/chart tests should keep that startup delay in mind.
- 2026-06-01 - Issue 14, Add chart parse canary: added `ChartParseCanaryTests.KnownGoodChartParsesThroughGameSongDetailPath`, using the deterministic `MAJTITLE` chart selected by Issue 13. The test waits up to 75s for `SongStorage` to expose `MAJTITLE`, calls the game's normal `ISongDetail.GetMaidataAsync(true)` path, then parses each raw `Fumen` through `MajSimai.SimaiParser.ParseChartAsync` until it finds a chart with timing points. It asserts the song is found, title is stable, parse succeeds, chart count is at least 5, selected chart index is in the expected 0-4 range, timing point count is nonzero, note count is nonzero, BPM timing count is nonzero, and first BPM is positive. Verification: `dotnet build mod-test-tools/integration/ModTest.Integration.Tests.csproj -c Release --nologo` passed with 0 warnings/errors; `sample-mod/test-integration.ps1` passed 6/6 serialized real-game xUnit tests in 1m35s and wrote `.scratch/mod-test-tools-artifacts/integration-test-results/integration.trx`; current `chart-parse`, `asset-data`, `scene-objects`, `game-types`, `integration`, and `boot-stable-state` `*.log` artifacts had no fatal/error scan hits. Surprises: `SimaiFile.Charts[i].Fumen` is a raw string, so the canary must explicitly call `SimaiParser.ParseChartAsync`; the eval compiler cannot emit async state machines in this profile, so async game APIs are invoked with `GetAwaiter().GetResult()` inside a non-async eval snippet; `ReadOnlySpan<SimaiTimingPoint>` also required plain indexed loops rather than LINQ.
- 2026-06-02 - UI prototype template issue 01, Scaffold runnable prototype mod: added `ui-prototype-template-mod` with a plain MelonLoader adapter, documented `build.ps1`/`install.ps1`, and removal instructions. The mod uses `[HarmonyDontPatchAll]`, logs `UI prototype takeover active - normal game UI is visually replaced.`, creates a full-screen prototype placeholder labelled `MAJDATA UI PROTOTYPE`, and installs into `Majdata Hub\game\Mods\UiPrototypeTemplateMod.dll`. Verification: `powershell.exe -NoProfile -ExecutionPolicy Bypass -File "C:\Users\user0-pc\majdataproj\ui-prototype-template-mod\build.ps1"` passed; `install.ps1` passed; a real MajdataPlay launch loaded `UI Prototype Template Mod v0.1.0` and logged `UI prototype canvas placeholder installed.` Caveat: the IMGUI path remains in the adapter, but this patched MelonLoader v0.4.3 profile only proved the startup-created Unity UI canvas fallback during the launch check, so later slices should keep the renderer isolated and may need to continue using the canvas fallback if `OnGUI` remains unavailable.
- 2026-06-02 - UI prototype template issue 02, Add pure prototype core and state tests: added `UiPrototypeTemplateMod.Core` with fake song/difficulty data, `PrototypePhase`, `PrototypeAction`, and `PrototypeSession`. The state machine now supports wrapping song navigation, OK from `SongSelect` to `DifficultySelect`, Back from `DifficultySelect` to `SongSelect`, OK to `Confirmed`, Back from `Confirmed`, per-song difficulty memory, and clamped difficulty movement that skips unavailable or locked difficulties. Fake data covers multiple categories, ranks, DX-score-like values, locked/unavailable difficulties, long-song and special-song flags, and badges. Added `test-unit.ps1` and 7 xUnit tests for the Unity-free core. Updated the adapter build/install to build and install `UiPrototypeTemplateMod.Core.dll` under `Mods\UiPrototypeTemplateModLib` and log the default core state at startup. Verification: `test-unit.ps1` passed 7/7 tests with coverage artifacts under `ui-prototype-template-mod\artifacts\unit`; `install.ps1` passed; a real MajdataPlay launch loaded the mod and logged `Prototype core ready: phase=SongSelect song=MAJTITLE difficulty=Basic`.
- 2026-06-02 - UI prototype template issue 03, Wire semantic input actions: added Unity-free semantic input mapping types in `UiPrototypeTemplateMod.Core` and connected the adapter to produce `RawPrototypeInput` from MajdataPlay A3/A6/A4/A5 reflection when available, otherwise keyboard fallback. The mapper converts A3/A6 differently by phase: song next/previous in `SongSelect`, difficulty up/down in `DifficultySelect`, with A4 as OK and A5 as Back. Keyboard fallback uses Right/Down/D, Left/Up/A, Enter/Space, and Escape/Backspace. Active input source is logged and included in overlay diagnostics. Verification: `test-unit.ps1` passed 11/11 tests; `install.ps1` passed; a real MajdataPlay launch loaded and rendered the mod after input wiring, logging `Prototype input source: Input: KeyboardFallback` and `UI prototype canvas placeholder installed.` Caveat: the launch-time reflection path fell back because Majdata input was not reflectable at mod startup in this profile; the reflection adapter remains in place for when the game input API is available during runtime updates.
- 2026-06-02 - UI prototype template issue 04, Render song-first fake selection flow: replaced the static placeholder with a song-first prototype canvas driven by `PrototypeSession`. The screen renders previous/current/next song panels, selected song title/artist/BPM/category, long/special/badge metadata, difficulty chips with rank and DX-score-like values, a phase-dominant panel for song browsing/difficulty selection/confirmed, phase-specific button prompts, and diagnostics. Added a synchronization-context timer pump so input/state refresh reaches Unity main thread even when patched MelonLoader update hooks are delayed; the semantic input path now drives song browsing, OK transition to difficulty selection, difficulty changes, confirmation, and Back navigation. Verification: `test-unit.ps1` passed 11/11 tests; `install.ps1` passed; a real MajdataPlay launch logged `UI prototype song-first canvas installed`, `Prototype input timer reached Unity main thread`, `UI prototype placeholder rendering through IMGUI`, and `UI prototype overlay behaviour is ticking`.
- 2026-06-02 - UI prototype template issue 05, Add three runtime variants: added `PrototypeVariantRouter` with exactly three variants and wraparound next/previous behavior, plus tests for variant count, cycling, and state preservation. The renderer now supports three meaningful layouts: a Sinmai-like carousel, a Majdata-native vertical list hybrid, and a compact fast-flow hybrid. Each displays a large `VARIANT N/3` label. Keyboard Left/Right are reserved for variant switching, so semantic keyboard fallback navigation now uses Down/D for next/harder and Up/A for previous/easier. Verification: `test-unit.ps1` passed 14/14 tests; `install.ps1` passed; a real MajdataPlay launch still loaded the mod, installed the song-first canvas, reached the Unity main-thread timer, rendered IMGUI, and ticked the overlay. Caveat: a synthetic Windows `SendKeys("{RIGHT}")` did not produce a variant-switch log in that launch run, so keypress switching is covered by the code path and unit-tested router rather than observed through the game window automation.
- 2026-06-02 - UI prototype template issue 06, Add install smoke verification: added `ui-prototype-template-mod/test-smoke.ps1` and documented it in the README. The smoke script stops stale Majdata/REPL processes, runs `install.ps1`, launches MajdataPlay through `start-controller.bat`, waits for prototype mod load/activation/core/render/main-thread timer log lines, scans current MelonLoader and Majdata runtime logs for fatal boot patterns, copies smoke artifacts under `ui-prototype-template-mod\artifacts\smoke`, and stops the game without manual UI interaction. Verification: `test-smoke.ps1` passed, confirming `UI Prototype Template Mod v0.1.0`, `UI prototype takeover active - normal game UI is visually replaced.`, `Prototype core ready: phase=SongSelect song=MAJTITLE difficulty=Basic`, `UI prototype song-first canvas installed.`, and `Prototype input timer reached Unity main thread.`
- 2026-06-02 - UI prototype template completion audit fix: found that this Unity profile does not expose `UnityEngine.Input.GetKeyDown` through the loaded input assembly, so the keyboard fallback and Left/Right variant switching path would not detect keys. Replaced that fallback with a Windows `GetAsyncKeyState` edge detector for the keys used by the prototype. Verification: `test-unit.ps1` passed 14/14 tests; `install.ps1` passed; `test-smoke.ps1` passed after the fix.
- 2026-06-05 - Majdata QoL song list mod issue 01, Scaffold production mod and core defaults: created the `.scratch/majdata-qol-song-list-mod/issues` backlog from the PRD and implemented the first production slice under `majdata-qol-song-list-mod`. Added a MelonLoader adapter, `MajdataQolSongListMod.Core`, pure map-list settings defaults/options, xUnit coverage for default values and supported option sets, and build/install/unit-test scripts. The core defaults are `Difficulty Filter=No`, `Sorting=Default`, `Grouping=Default`, and `Downloaded Songs Filter=Mixed`; grouping options deliberately omit `Version` and `All`. Verification: `test-unit.ps1` passed 6/6 tests with coverage output; `build.ps1` passed after rerunning serially because the first parallel build collided with the test process writing the same core output; `install.ps1` installed `MajdataQolSongListMod.dll` and `MajdataQolSongListMod.Core.dll`; a short MajdataPlay launch logged `Majdata QoL Song List core ready: difficultyFilter=No sorting=Default grouping=Default downloadedSongsFilter=Mixed` and `default folder behavior preserved`.
- 2026-06-05 - Majdata QoL song list mod issue 02, Catalog row and index deduplication: added the unified catalog model in `MajdataQolSongListMod.Core`, including `CatalogInput`, `CatalogRow`, `CatalogIndex`, `CatalogLevel`, `ScoreFacet`, `InteractionFacet`, `CollectionMembership`, source flags, preferred playback source, and hydration state. The index deduplicates local/online rows by normalized hash, keeps distinct hashes separate, prefers local playback for duplicate downloaded-plus-online rows, preserves online id/uploader/interaction/collection metadata, and keeps blank/missing-field rows instead of dropping them. Verification: `test-unit.ps1` passed 11/11 tests; `build.ps1` passed; `install.ps1` installed the updated mod and core DLLs.
- 2026-06-05 - Majdata QoL song list mod issue 03, Level, difficulty, and grouping core: added pure grouping behavior in `MajdataQolSongListMod.Core` with `LevelBucketizer`, `DifficultyCount`, `CatalogCollection`, `CatalogGroupingRequest`, and `CatalogNavigator`. Level normalization covers blank/malformed values, integer levels, decimals, plus levels, and boundary cases such as `13`, `13.0`, `13+`, `13.1`, and `14.0`; difficulty count filters use nonempty usable levels; default grouping passes through existing folder collections including `All`/`MyFavorites`; alternate grouping covers difficulty bracket, selected-difficulty level, artist with `Unknown Artist`, title/name with `#` and `Other`, and rank with `No Play`. Verification: `test-unit.ps1` passed 36/36 tests; `build.ps1` passed; `install.ps1` installed the updated mod and core DLLs.
- 2026-06-05 - Majdata QoL song list mod issue 04, Virtual folders for Random Recommend and favorites: extended `CatalogCollection` with tile text, selected-info text, virtual state, and online-backed state, then added `VirtualCollectionFactory` and wired it through `CatalogNavigator`. Default folder browsing and every supported grouping mode now ensures `MyFavorites` and `Random Recommended` are present; default mode inserts them after `All`, `Random Recommended` follows `MyFavorites`, and pre-existing favorites are not duplicated. `Random Recommended` exposes tile text `Random\nRecommended`, selected info `Random Recommended`, virtual state, and online-backed state for the later UI patch. Verification: `test-unit.ps1` passed 45/45 tests; `build.ps1` passed; `install.ps1` installed the updated mod and core DLLs.
- 2026-06-05 - Majdata QoL song list mod issue 05, Sort core: added `BpmFacet`, `CatalogSortMode`, `CatalogSortRequest`, and `CatalogSorter`. The UI sort option set remains aligned with the proposal and omits unsupported Sinmai-only labels, while the pure core supports default, date added, difficulty, note designer, title, rank, artist, play count, BPM, AP/FC rank, and internal DX score sorting. Sorts use deterministic title/hash tie-breakers; local play count sorts immediately; confirmed online zero play count remains distinct from unknown; known BPM sorts before pending/unknown BPM; stale known values remain usable; AP/FC and DX score sorts degrade cleanly when score facts are missing. Verification: `test-unit.ps1` passed 52/52 tests; `build.ps1` passed; `install.ps1` installed the updated mod and core DLLs.
- 2026-06-05 - Majdata QoL song list mod issue 06, MajdataNet adapter and Random Recommend fetch: added `MajdataNetAdapter`, `ITextFetcher`, `HttpTextFetcher`, recoverable `MajdataNetResult<T>`, `RandomRecommendationRequest`, `RandomRecommendationBatch`, and `RandomRecommendationService`. The adapter converts representative `/api/maichart/list` JSON fields (`id`, `title`, `artist`, `designer`/`designers`, `uploader`, `levels`, `timestamp`, `hash`) into online catalog inputs, uses the unauthenticated chart-list endpoint, and treats fetch failures as recoverable results. Random recommendations are built from fetched chart-list rows with deterministic seeded shuffling, vary across seeds, and can fall back to configured local catalog rows when online fetch fails. Verification: `test-unit.ps1` passed 59/59 tests; `build.ps1` passed; `install.ps1` installed the updated mod and core DLLs.
